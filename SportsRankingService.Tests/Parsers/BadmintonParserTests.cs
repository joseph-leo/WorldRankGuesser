namespace SportsRankingService.Tests.Parsers;

public class BadmintonParserTests
{
    [Fact(Skip = "No fixture: bwfshuttleapi.com does not respond (captured 2026-09-15). Needs a new source, e.g. the bwfbadminton.com rankings API.")]
    public void Parses_singles_rankings()
    {
    }
}
